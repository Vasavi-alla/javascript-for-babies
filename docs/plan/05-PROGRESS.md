# 05 — Progress (source of truth for day-to-day work)

> Update this file at the end of every session: statuses, next-up list, session log.

## Status board
| Milestone | Deliverable | Status |
|---|---|---|
| Planning | Plan files (00–05), CLAUDE.md | ✅ done (2026-07-02) |
| M0 | Scaffold: Vite + React + TS, Tailwind, fonts, design tokens, paper texture, core design components (PaperCard, RoughBox, HandArrow, StickyNote, InkButton, CodePane, ConsolePane, Stepper), app shell + router, lesson registry, Stepper engine, curriculum map + per-phase pages | ✅ done (2026-07-02) |
| M1 | Phase 0 (5 lessons) + Phase 1 (10 lessons + checkpoint). Hero build: `MemoryDiagram` | ✅ done (2026-07-02) — all of Phase 0 (0.1–0.5) and Phase 1 (1.1–1.11) built; user verified through 1.4, later lessons pending user verification |
| M2 | Phase 2 + Phase 3 (FlowRoad, LoopMachine, FunctionMachine, ScopeLens, CallStackTower, ClosureBackpack) | ✅ done (2026-07-03) — Phase 2 (2.1–2.8) + Phase 3 (3.1–3.11) all built in classic format with typed checks + in-lesson CodeExercises; pending user browser verification |
| M3 | Phase 4 + Phase 5 (heap-mode MemoryDiagram, PipelineBelt, TwoPassScanner, ThisCompass, PrototypeChain) | ✅ done (2026-07-04) — Phase 4 all 14 (incl. NEW 4.2 array-memory/O(1) + 4.5 hash lessons per depth feedback; 4.12 "Map, Set & JSON" later split into 4.12 Map & Set + 4.13 JSON, checkpoint renumbered 4.14), Phase 5 all 9 |
| M5′ | Phase 6 async (BlockedStack, EventLoopMachine, PromiseReceipt, TwoQueues, AsyncShelf, NetworkRoundTrip, RaceLanes) — runner upgraded to async (await/timers captured) | ✅ done (2026-07-04) — 6.1–6.9 |
| M6′ | Phase 7 DOM | ✅ done (2026-07-04) — 7.1–7.9 all built (tree, selectors, mutation, events, bubbling/delegation, forms, storage/timing, rendering pipeline, todo-app checkpoint) |
| M4 | Sandbox v1 (acorn + step-evaluator; variables/functions/control flow) | ⬜ not started |
| M5 | Phase 6 (EventLoopMachine 🎬, PromiseTimeline, NetworkRoundTrip) + sandbox async | ⬜ not started |
| M6 | Phase 7 + Phase 8 (DOMTree, SelectorLab, EventBubbles, ModuleGraph) | ⬜ not started |
| M7 | Phase 9 + Phase 10 (TestPyramid, AssertionScale, TestRunnerPane, PlaywrightBridge, SelectorLab v2) | ⬜ not started |
| M8 | Polish: recall deck, journal review, progress export, reduced-motion & a11y pass, optional chalkboard theme | ⬜ not started |

## Next up (top = do first)
0. **Fresher read-through ROUND 2 complete (2026-07-04) — report at `docs/reviews/2026-07-04-fresher-review-round-2.md`, fixes NOT yet applied.** All 76 lessons (Phase 0–7) re-read as a zero-experience learner after the round-1 fixes; every surface checked (captions cross-checked against per-step viz code, underTheHood, quizzes, teach-backs, recaps, exercises). Verdict: strong; round-1 fixes landed. Key finds, by severity: (1) HIGH — 0.2's "where does your JS run?" step is a dead prediction: the caption promises a question that never appears and the visitor's-machine fact is taught nowhere visible, yet the teachBack asks it; (2) MED systemic — orphaned "Predict…" captions in Phases 0–3 (dead prediction fields); 2.3's grade-95 question and 2.6's colon-into-nothing caption never get answered on screen; (3) MED — 2.7's viz shows the answer (console `1 2 4 5`) during its own predict step; (4) MED — teachBack MODEL ANSWERS in 2.6 (FORTRAN) and 2.7 (Dijkstra) still cite fun facts removed from the lessons; (5) MED — stale renumbering refs: 4.9 says "(4.2)" for push/pop (now 4.3), 4.8 says "(4.4 forever)" for arrows (now 4.6), and 4.14 has a literal "lesson 2.x" placeholder (should be 1.10); (6) MED — F12 instructions (0.3, 1.11, 2.8, 4.13) impossible on iPad, a primary device; (7) MED-LOW — Phase 7's HTML gap scoped: intro primer covers concepts, but tag VOCABULARY (h1/ul/li/p…) is glossed nowhere — recommended: one gloss beat in 7.1 + a 10-tag mini-glossary in the phase intro (no new lesson needed); (8) LOW — ~13 frozen "nothing changes on next" steps in Phases 0–4 (badge? never backported), 2.2 falsy-chips reveal desync, small gloss gaps (heap in 3.7, "desugar" 5.6, "two new friends" 1.5). Report has a 3-batch prioritized action list. **Round 2.1 (same day, user-prompted):** user flagged 1.4's const "welds the label, not the box shut" fine print and 1.6's train as confusing → diagnosed two new defect classes and swept ALL 76 lessons for them: (a) forward-reference notes must FLAG, never TEACH future mechanics ("preview, not homework") — new finds: 1.7's `==`/`===` preview (symbols before 1.9 teaches them), 3.5's TDZ fun fact ("the variable exists but may not be touched" pre-teaches 5.1's registration and contradicts 3.5's own bubble model); (b) metaphors need a "here the picture ends" line at breaking points — new find: 2.2's funnel never says the original value is untouched. Also caught **4 more stale phase numbers from the Node insertion** (0.2 "Phase 10"→11; 1.5, 2.8, 3.11 "Phase 9"→10); all forward lesson numbers (9.6/9.7/10.5/11.3/11.5/11.7/11.8) verified against 01-CURRICULUM.md. Everything else (3.4/3.8/5.8/4.12 notes; backpack/receipt/boxes/machine metaphors) verified clear. Findings A10–A12 in the report; new standing rule saved to memory (feedback-forward-refs-and-metaphor-limits). **BATCH 1 APPLIED (2026-07-04, ~25 files, build green, NOT committed):** 0.2 dead-prediction step rebuilt as ask→answer steps + viz beat (node threshold 6→7); 2.3 gained an "order-answer" step (viz thresholds <=2→<=3, fell >=4→>=5); 2.6/0.5/2.7/2.8 questions now answered in-caption; ALL remaining "Predict."-style imperatives reworded to plain questions (0.3, 0.4, 1.2, 1.3, 1.4, 1.5, 1.7, 1.8, 1.9, 1.11, 2.1, 2.4, 2.5); stale refs fixed (4.9→4.3, 4.8→4.6, 4.14 2.x→1.10, 0.2 Phase 10→11, 1.5/2.8/3.11 Phase 9→10); FORTRAN/Dijkstra removed from 2.6/2.7 model answers; 1.4 const fine print → flag-don't-teach ("A preview, not homework", defers to 4.6); 1.6 train boundary line added; 2.2 funnel "only READ, never changed"; 1.7 ==/=== preview now glosses before symbols; 3.5 TDZ fun fact rewords to observable behavior; 3.7 heap gloss + React name-drop removed; 3.8 "points at"; 5.6 + phase-intros desugar gloss; iPad alternatives added to all four F12/DevTools instructions (0.3, 1.11, 2.8, 4.13). **BATCHES 2 + 3 ALSO APPLIED (2026-07-04, build green, NOT committed):** Batch 2 — 2.7's predict step restored as a real question with a NEUTRAL viz state (new `pending` prop on StationTrack: no printed labels, console shows "?", "i === 3 → continue… then what?"), answer reveals next step; 2.2's falsy chips now appear 3-then-3 matching the two captions; 7.1's element-nodes caption glosses h1/ul/li/body; Phase-7 intro gained a "tags you'll keep meeting" keyTerm (10 tags). Batch 3 — badge annotations added to the 12 genuinely frozen steps (1.8 roundup-safe, 2.1 else-optional, 2.2 wrap×2, 2.3 rule×2, 2.4 taste, 2.7 grid-scope via GridDraw badge prop, 3.10 wrap, 4.1 length-offbyone + const-tease via View.badge, 4.4 bracket-dynamic-why via View.badge); 4.12 verified NOT frozen (per-step note already changes) and skipped; 2.8 gained a NEW "order-trap" step — the corridor physically reorders (%3 first), 15 gets grabbed → "Fizz", %15 gate labeled "never consulted: dead code, forever" (steps 7→8, WALK 7→8 entries, showRun >=7, hand-verified). **Next: user browser-verifies. Priority spot-checks: 0.2 (9 steps, question/answer beats), 2.3 (9 steps, new answer step), 2.8 (8 steps, the reordered-corridor trap at step 5), 2.7 (neutral predict picture at step 3, badge on last step), 2.2 (chips arrive 3-then-3, closing badges), plus one badge lesson like 4.1 or 4.4. All review-report items are now applied; nothing committed.**
1. **Learner-name personalization built (2026-07-04) — awaiting user browser verification.** The home-page greeting name is now click-to-edit (dashed underline + ✏️), saved to localStorage under `jfb-learner-name` via a new `useLearnerName()` hook in `src/content/learner.ts` (default: "friend"; empty input falls back to it; 30-char cap). Saving a real name also rewrites the address bar to `/<name-slug>-journey` (cosmetic — the route renders the same CurriculumMap and never reads the slug; `/` still works, no redirect). Implementation note: react-router 8 does NOT support partial dynamic segments (probed: `:slug-journey` parses as a param *named* "slug-journey" matching any segment), so the route is `path=":slug"` + a `JourneyRoute` wrapper that renders CurriculumMap only when the slug ends in `-journey` (null otherwise — unknown single-segment URLs now show the Layout shell instead of a blank page, accepted in spec). Spec: `docs/superpowers/specs/2026-07-04-learner-name-personalization-design.md`. Verify per the checklist in `docs/superpowers/plans/2026-07-04-learner-name-personalization.md` Task 3 Step 4 — especially Escape-cancel and the `/lijas-journey` refresh.
-3. **Full-curriculum beginner read-through (2026-07-04) — 28 findings, quick-fix batch APPLIED, awaiting user browser verification.** User asked Claude to role-play a total-zero-experience learner (no coding background, goal = Playwright automation tester) and read all 76 built lessons (Phase 0–7) cover to cover. Dispatched as 6 parallel subagent passes (Phase 0-1, 2-3, 4, 5, 6, 7); full report lives in that session's transcript/artifact. User then said "yes" to fixing the quick batch immediately and brainstorming the one bigger item separately. **Fixed this session:**
  - **Phase-numbering bug (Playwright):** 7.9's hardcoded "Phase 9 is Testing, Phase 10 is Playwright" reworded to match `registry.ts` (9=Node.js, 10=Testing, 11=Playwright). `src/content/phase-intros.ts` restructured: new `[9]` Node.js intro written from scratch (whyNeeded/plainWords/keyTerms/youCan, matching curriculum.md's Phase 9 lesson list), old `[9]` Testing content moved to `[10]`, old `[10]` Playwright content moved to `[11]`; `[8]`'s (Modern JS & Tooling) Node.js over-emphasis trimmed since Node now has its own phase.
  - **3.10 content gap:** added a genuine rest-params (`...numbers`) teaching beat — new `REST_CODE` sample, 2 new steps + matching `Scene`/render branch in the Viz, a quiz question, recap bullet, and hook/underTheHood/teachBack updates. Was previously title-only.
  - **Continuity errors fixed:** 5.4's hook no longer claims 3.4 "promised" `this` (3.4 never mentions it) — reworded honestly. 7.1's "4.12 taught you: wires carry text" corrected to 4.13 (JSON).
  - **4.6 (reference-semantics, the highest-stakes lesson):** added a bridging sentence tying "the stack" to 3.9's call stack; added a genuinely new, animated `reassign-param` step (new `CODE_C`, a second heap-box in `HeapDiagram`, matching `View`/quiz additions) so the reassign-doesn't-reach-the-caller rule is watched, not just asserted in prose.
  - **4.2:** reconciled the `big[50]=1` aside with the "must stay contiguous" claim in the same step instead of leaving the tension for underTheHood to resolve two sections later.
  - **Jargon glossed on first use:** operand (1.9), API (1.7), `%`-as-remainder (4.5), DNS + content-type (6.7), `.matches()`/`.closest()` (7.5).
  - **Weak fun facts fixed** (no new facts invented, per standing rule): 0.2 split its bundled V8/Ignition/TurboFan fact into two paragraphs; 2.2 (`document.all`), 2.6 (FORTRAN "i"), and 4.12 (pre-2015 Map/Set history) removed outright; 2.7 (Dijkstra) trimmed to drop the bare person/year while keeping the break/continue lineage point.
  - **Ambiguous wording fixed:** 6.4 quiz 3 now specifies the promise the chain *starts from* rejects; 5.9 quiz 4 now asks for "the FIRST console.log line" in the question text itself, not just the placeholder; 7.9's inspect-selectors step now says the sandbox models the app as pseudocode rather than implying a live page exists to open DevTools against.
  - **Forward-reference gaps fixed:** 1.4 now glosses function-scoped vs block-scoped in plain words; 2.5's "remember the Phase 6 preview" (which never happened) reworded to just defer to Phase 6 honestly; 7.4's recap no longer claims "submit" was covered in 7.4 (it's 7.6).
  - Also added classes + try/catch/finally to `PHASE_INTROS[5]`'s keyTerms/youCan (were missing despite 5.6/5.8 teaching them) — found during the numbering fix.
  - Every touched file rebuilt (`tsc -b && vite build`) clean; hand-verified `VIEWS.length === steps.length` for 3.10 (8/8) and 4.6 (8/8), the two files with new steps.
  - **NOT fixed yet — deliberately deferred:** Phase 7's missing HTML/CSS baseline literacy (7.1's first markup sample, "CSS" never defined) is a bigger design call (add a primer lesson vs. inline glosses), to be brainstormed with the user separately before touching it.
  - **Next: user runs `npm run dev` and spot-checks 3.10 (new rest-params steps) and 4.6 (new reassign-param step, the second heap box) especially**, plus a sample of the smaller text-only fixes, before this is considered done.
-2. **Curriculum-wide "too much information" density pass — COMPLETE, awaiting user browser verification (2026-07-04).** User flagged that a pasted strings excerpt (1.6) and the Numbers lesson (1.5) crammed too many facts into single paragraphs/steps — asked for an audit "from phase 1 to all." Background audit confirmed the pattern was curriculum-wide (Phase 0 through 7, all 75 lessons at the time), not phase-specific: the dominant defect is `hook`/`underTheHood` paragraphs bundling 2-5 facts with no room between them (a "toolbox" list of methods/operators/terms in one breath, or a definition+caveat+forward-reference stack); a secondary, milder defect is "wrap"/recap steps smuggling one genuinely new fact alongside the recap. Fixed by splitting dense paragraphs into more, shorter, single-idea paragraphs (superseding the old "3 paragraphs / ~250 words" underTheHood rule — paragraph *count* is no longer capped, one-idea-per-paragraph is what matters now) and, in the clearest cases, splitting a step's caption into two steps (adding a matching VIEWS/SCENES entry, verified by rebuild each time). **Checkpoint lessons (1.11, 2.8, 3.11, 5.9, 6.9, 7.9) were explicitly exempted** — their "everything in one breath" closing recap is a deliberate synthesis device, not the accidental-cramming defect, though non-recap density elsewhere in those files (e.g. 2.8's underTheHood, which teaches new "boundary testing" vocabulary, not recap) was still fixed. **Lesson 4.12 ("Map, Set & JSON") was split into two lessons** — 4.12 now Map & Set only, new 4.13 is JSON on its own (fresh Viz `JsonBridge`, fresh underTheHood, 8 steps) — and the old checkpoint moved from 4.13 to **4.14** (`lesson414.tsx`, content unchanged). Updated `registry.ts` (new 4.12/4.13/4.14 entries + slugs, Phase 4 `plannedLessons` 13→14), `index.ts`, `01-CURRICULUM.md`'s Phase 4 table and the stale 11.8 cross-reference, and every stale "4.12" JSON forward-reference found elsewhere (4.7, 4.9, 6.7 ×2, 7.7 ×3). **Drive-by bugs found and fixed along the way** (same undetected-by-tsc class as the earlier stepper rollout): 2.8's `WALK` array was off-by-one (the "walk i=3"/"walk i=5" steps silently redisplayed the blank corridor instead of walking those values). Every touched file was rebuilt (`npm run build`) after editing; all green. **Next: user runs `npm run dev` and spot-checks a sample across phases — especially 1.5, 1.6, 1.10 (heaviest rewrites), the new 4.12/4.13/4.14 lesson trio, and 2.1/2.2/2.7/2.8 (where steps were also split) — before this is considered done.**
-1. **Phase 7 complete — 7.5–7.9 built (2026-07-04), awaiting user browser verification.** Built the remaining 5 DOM lessons in the same classic format/pacing established through 7.1–7.4: **7.5** Bubbling, delegation & preventDefault (10 steps; EventBubbles — a marker travels capture↓/target/bubble↑ through a 4-node tree; delegation via one listener + event.target, covers future children too; stopPropagation vs preventDefault distinguished). **7.6** Forms & user input (9 steps; FormPanel — value/checked reading, submit's default reload, preventDefault, read→validate→act guard chain, Playwright fill/check/selectOption tie-in). **7.7** Storage & timing (9 steps; StorageTimeline — localStorage vs sessionStorage vs cookies, JSON round-trip, DOMContentLoaded vs load timeline, foreshadows auto-waiting). **7.8** How the browser renders (9 steps; RenderPipeline — Parse→Render Tree→Layout→Paint ribbon, display:none exclusion from the render tree, the "exists in DOM ≠ visible" gap that motivates Playwright's auto-waiting). **7.9** Checkpoint: todo app, inspected (8 steps; synthesis walkthrough tying all of 7.1–7.8 into one small app, then "inspect what you built" with 7.2's selector skill; two exercises — pure add/toggle logic, then a JSON-parse reload-survival drill). All five registered in `src/lessons/index.ts` + flipped to `'available'` in `src/content/registry.ts`. Caught and fixed a recurring typo class while building (straight apostrophes inside JS string literals break the string — must use curly ’ in caption/note/badge/quiz/teachBack/recap text; JSX text between tags is unaffected) — checked every file with a grep pass before each build. Every file: hand-verified VIEWS.length === steps.length, then `npm run build` green. **Next: user runs `npm run dev` and browser-verifies 7.5–7.9 (especially 7.5's capture/bubble marker animation and 7.8's render pipeline ribbon).**
0. **Phase 5-7 stepper pacing rollout — COMPLETE, user-verification still pending (2026-07-04).** Full rollout finished: all 22 built lessons across Phase 5 (5.1–5.9), Phase 6 (6.1–6.9), and Phase 7 (7.1–7.4) now have expanded "watch it happen" steppers (5-6 steps → 7-12, matching the blueprint's range) with the metaphor bridge-then-fade rule applied to the 7 sustained-metaphor lessons (5.3, 5.4, 5.7, 5.8, 6.1, 6.4, 6.6, 6.8). Mid-rollout, the user reported 5.4's compass-needle animation was "rotating in a weird manner, didn't understand what was happening" — root cause: the needle's rotation angle was an arbitrary number per rule with no real meaning, so it swung unpredictably. Fixed by replacing the compass entirely with a **growing comparison table** (one fixed function `whoAmI`, called 4 different ways, filling in "called as / this becomes / why" rows one at a time) — no rotation, nothing to misinterpret. This also surfaced a systemic pattern: several lessons (5.4, and later found in 6.7, 7.2, 7.3) had a pre-existing **steps/VIEWS array length mismatch** (more narrated steps than visual states), silently showing the WRONG picture on later steps since `tsc` can't catch this (no `noUncheckedIndexedAccess`). All of these were fixed as part of the rollout. Standardized a `badge?: string` field across every lesson's View interface: elaboration-only steps (added insight without a new core visual) now show a small appearing annotation instead of leaving the picture static — this is the fix for the general "frozen picture, nothing happens on next-click" defect class. Every single file was verified: hand-counted VIEWS/steps array lengths match, then `npm run build` green. **Next: user runs `npm run dev` and spot-checks a handful of lessons across all three phases (especially 5.4's new table, and any of 5.3/5.7/5.8/6.1/6.4/6.6/6.8 for the metaphor fade) before this work is considered done.**
1. **Phase 4 in progress — 4.1–4.4 built (2026-07-04). The hero lesson 4.4 is DONE** (HeapDiagram: stack slots vs heap, arrow copies, aliasing in daylight, const-locks-the-arrow payoff, call-by-value vs call-by-sharing in two acts via codeOverride, Google-Docs-link-vs-attachment fun fact, scoreboard exercise with === identity proof). **Next: 4.5 Copying & equality** (spread shallow copy, inner arrows still shared, {} !== {} — 4.4's underTheHood explicitly teed it up), then 4.6–4.11 (d3 at 4.7 PipelineBelt), then Phase 5 (owes hoisting/`this` from 3.3/3.4). Barnaby is now HOME-PAGE ONLY (user request — was distracting in lessons).
2. User should browser-verify 3.1–3.11 and new 4.1–4.3 (especially 4.2's sliding re-index animation and the three exercises on iPad). NOTE: curriculum grew a dedicated **Phase 9 Node.js** (2026-07-04); Testing→10, Playwright→11.
3. Metaphor note: 3.1–3.5 still lead with the machine picture (fine for the gradient's transition zone); 3.6–3.11 lead with real terms. Only re-voice earlier lessons if the user asks.
2. Phase 2 lab (`src/labs/`) still missing — phases 0/1 have labs. Consider a LoopMachine playground.
2. **Fun facts are OPTIONAL (user refinement 2026-07-03: "not all facts are fun")** — include a "Fun fact:" only when it's genuinely story-worthy (V8 naming, typeof-null 1995, AT&T 1990); skip dry terminology/spec history entirely. Never invent facts. See memory: feedback-fun-facts.
2. Before starting M2, have the user run through 1.5–1.11 in the browser (not yet user-verified) and collect feedback — especially the 1.11 MadLibsBuilder interactive.
3. User asked (2026-07-02) about objects & call-by-value/call-by-reference: confirmed it's Phase 4 (lesson 4.4 updated in 01-CURRICULUM.md to explicitly cover call by value vs call by reference/sharing for function arguments). Honor this when building Phase 4.
4. Note: user prefers to run/verify the app themselves; don't auto-run builds without need.
2. Lesson framework already exists: `engine/lesson/LessonShell.tsx` renders a `LessonDef` (see `engine/lesson/types.ts`); lessons live in `src/lessons/phaseN/lessonNN.tsx`, are registered in `src/lessons/index.ts`, and their registry status flipped to 'available' in `src/content/registry.ts`. Follow the existing Phase 0 lessons as templates (0.1 is the reference).
3. Progress store (`src/store/progress.ts`): completion + teach-back journal, persisted. A journal review page is still to build (M8).
4. Caution: don't edit `src/content/registry.ts` with PowerShell regex — it corrupts UTF-8 (happened once; file was rewritten). Use the Edit tool.

## Standing user feedback (apply to ALL future work)
- **Metaphor weaning (2026-07-03).** Training-wheel metaphors must fade as the learner matures: Phases 0–1 metaphor-first ("boxes", "labels", "machines"); Phases 2–3 real term first with the metaphor in parentheses ("memory (the boxes)", "function (the machine)"); Phase 4+ real terms — metaphors only as occasional callbacks. Never introduce a new concept in Phase 4+ with a metaphor as its primary name.
- **Hands-on beats clicking (2026-07-03).** Writing real code that really runs is the core learning act; click-interactions support it, never replace it. **Exercises live INSIDE lessons** (`PlayExtra` CodeExercise; separate practice-bay pages were tried and removed — "very less and at the very last"). Not every lesson needs one; place each where its concept lands, cumulative where possible (functions exercises re-test let/const/if/loops).
- **Fun-fact standard (2026-07-03).** A fun fact must have an everyday-world hook — V8 named after the car engine, "Java is to JavaScript as ham is to hamster", AT&T's 9-hour collapse from one break. History trivia (person + year + language) reads as "a fact, not fun" and gets replaced: Eich-10-days was swapped out; 3.3 Dartmouth-BASIC and 3.4 CoffeeScript are flagged for the same treatment in the 3.3–3.5 refresh.
- **Teach first, then work (2026-07-03).** A mission's first step is KT — explain the syntax/anatomy before asking the learner to build anything.
- **Typed checks, silent steppers (2026-07-03).** "Watch it happen" never asks questions (prediction gates removed engine-wide; the `Step.prediction` field is dead). Lesson checks are typed-input (`kind: 'type-output'`, exact answers + accepted variants) wherever a concrete value/word/count exists; MCQ only where the options are the teaching (e.g. 0.1+0.2 WHY, var-tutorial judgment, L01 metaphors).
- **Exercises must be TRANSFER, never clones (2026-07-03).** An in-lesson exercise may not re-skin the watched code (watch greet → write welcome was rejected). Same concept, different surface, PLUS at least one new twist the stepper didn't show (a loop in the body, a computation instead of gluing, two calls to one function, two callbacks, values combined with +…). Applied: 3.1 cheer, 3.2 weather-order-trap, 3.3 half(10)+half(6), 3.4 arrow+ternary bigger, 3.6 chorus-twice, 3.8 fullDay(two callbacks), 2.1 pass/fail with >=.
- **Exercise wording = LeetCode style (2026-07-03).** Story → precise requirements (exact names, exact output, constraints — the WHAT) → exact expected output block. Never step-by-step code instructions (the HOW is the learner's job; inspection labels and show-answer teach it).
- **No prose walls in notes (2026-07-03).** Shop notes / under-the-hood as scannable sticky-note cards with bold headers, not paragraphs.
- **User's example phrasings are direction, not copy** — rewrite in the app's own voice.
- **Branding rule (2026-07-03):** app-level identity = the notebook/sketchbook ("your notebook", "this notebook keeps your page"). Phase worlds (Machine Shop, Warehouse…) are per-phase costumes and must never leak into app-level copy (home page, break coach, stickers, TeachBack shell).
- **Interaction clarity rules (2026-07-03, from 3.1 review):** (a) clickable things must look unmistakably clickable — never low-opacity tints on cream paper; (b) every challenge = short prompt + ONE highlighted "👉 do this" line (people skim); (c) animations must show the actual work happening (assembly line gluing the sentence), never abstract stand-ins like gears; left-to-right flow reads best; (d) ALL interactive progress persists — challenges, exercises — revisits resume, never restart.
- **Write for true newbies.** No expert one-liners anywhere in the UI; every phase/lesson listing carries a plain-words explanation. Phase intros live in `src/content/phase-intros.ts`.
- **Motivation before mechanism.** Every phase (and lesson) opens with "why do we need this?" — what breaks without the concept (`whyNeeded` in phase-intros).
- **Foundations get priority.** Phases 0–1 are visually elevated ("start here — the foundation").
- **Name and explain runtime concepts explicitly** — synchronous, asynchronous, blocking, non-blocking, single-threaded, Node.js's model (Phase 6 + 8 intros do this; lessons must too).
- Structure: compact home map → rich per-phase pages, not everything on one page.
- **Labs are per phase** (`src/labs/`, rendered on phase pages). `/design` is only the app's internal style guide.
- **Prediction/quiz wording must stand alone** in plain language — never depend on a metaphor from an earlier step (e.g. say "keeps age but swaps what it remembers", not "the label moves to a brand-new box").
- **Keep "Under the Hood" compact** (user request 2026-07-03): 3 short paragraphs max (~250 words total incl. the fun fact), matching Phase 0–2 lessons. Long walls of text lose the learner's attention. See memory: feedback-compact-under-the-hood.

## Working agreements
- Verify every lesson in the browser before marking done.
- After each milestone, pause for the user to actually *use* the lessons and give feedback before starting the next.
- Lesson content lives in the app (React), plans live here in markdown.

## Session log
| Date | What happened |
|---|---|
| 2026-07-04 | **Fresher read-through round 2 (all 76 lessons, Phases 0–7).** Role-played a total-zero learner through every lesson surface — including cross-checking each stepper caption against the viz code for that exact step, which is how the subtle finds surfaced: 0.2's core question lives only in the dead prediction field (HIGH); 2.7's viz spoils its own predict step; 2.6/2.7 model answers cite removed fun facts; 4.8/4.9 carry stale pre-renumber refs and 4.14 a literal "2.x" placeholder; ~13 frozen elaboration steps in Phases 0–4 lack the badge pattern; Phase 7's HTML gap narrowed to tag vocabulary only. Also catalogued what works (promise/payoff threading, build-the-tool exercises, window-vs-chute) as templates. Full report + 3-batch action plan: `docs/reviews/2026-07-04-fresher-review-round-2.md`. No lesson files touched — findings only, per request. |
| 2026-07-04 | **Learner-name personalization.** LEARNER_NAME constant retired; `useLearnerName()` hook (localStorage `jfb-learner-name`, default "friend") + click-to-edit name in the CurriculumMap headline (Enter/blur saves, Escape cancels via a done-ref so the trailing blur can’t double-fire; iPad input attrs; 30-char cap) + cosmetic `/<name>-journey` route in App.tsx. Probed react-router 8’s matchPath before choosing the route shape: partial dynamic segments are unsupported (`:slug-journey` = a param named "slug-journey"), so used `path=":slug"` + a JourneyRoute suffix-check wrapper; route-ranking probe confirmed /design, /phase/:n, /lesson/:id all still win over :slug. Build green; browser checklist pending user. |
| 2026-07-04 | **Phase 7 complete — built 7.5 through 7.9**, closing out the DOM phase. 7.5 Bubbling/delegation/preventDefault (capture→target→bubble journey visualized as a marker sliding down/up a 4-node tree; event.target vs event.currentTarget; delegation covers future children). 7.6 Forms & user input (value vs checked, submit's default reload, the read→validate→act guard chain reusing the 5.8/6.7 error-guard pattern). 7.7 Storage & timing (localStorage/sessionStorage/cookies contrasted, JSON round-trip, DOMContentLoaded vs load). 7.8 How the browser renders (Parse→Render Tree→Layout→Paint, display:none's render-tree exclusion, the exists-vs-visible gap motivating Playwright's auto-waiting). 7.9 checkpoint (todo app walkthrough synthesizing all of 7.1–7.8, then "inspect what you built" with 7.2's selectors; two exercises — pure-function add/toggle logic, then a JSON.parse reload-survival drill). All five follow the same conventions established this session: 7-10 step range, one fact per step, a `badge?` field for elaboration-only steps so nothing ever looks frozen on "next." Registered in `lessons/index.ts` and flipped to `'available'` in `content/registry.ts`. Hit and fixed a new mechanical gotcha twice while authoring fresh content (not found during the earlier editing pass, since that only touched existing correctly-quoted text): straight apostrophes inside JS string literals (captions, quiz `why`, teachBack, recap) are syntax errors — must be curly ’ — while the same character is harmless in JSX text between tags; added a grep check before each build going forward. Status board M6′ now ✅ done. |
| 2026-07-04 | **Phase 5-7 stepper-expansion full rollout (all 20 remaining lessons).** After the pilot (5.4, 6.2) was verified, applied the identical step-splitting + metaphor-fade pattern to the rest: Phase 5 — 5.1 (6→10 steps), 5.2 (5→8), 5.3 (5→8, rope→"outer link" fade), 5.5 (6→9, added a genuine total-chain-miss step + fixed the seek-badge to show a real "not found" state), 5.6 (5→8), 5.7 (5→8, janitor/broom fade), 5.8 (5→8, spark/net fade), 5.9 checkpoint (5→8). Phase 6 — 6.1 (6→9, hostage-metaphor fade), 6.3 (5→8), 6.4 (6→9, receipt/kitchen fade), 6.5 (6→8), 6.6 (6→8, shelf/bookmark fade), 6.7 (6→9 — also fixed a pre-existing 6-steps/5-VIEWS mismatch that was silently showing the wrong picture from the 'guard' step onward), 6.8 (5→9, kitchen-lanes fade), 6.9 checkpoint (5→8). Phase 7 — 7.1 (5→9, on top of the earlier listener-wording fix), 7.2 (5→9 — fixed a pre-existing 5-steps/6-VIEWS mismatch that broke 3 of 5 steps' pictures), 7.3 (5→7 — fixed a pre-existing 5-steps/6-VIEWS mismatch), 7.4 (5→9). Mid-rollout the user flagged 5.4's compass needle as "rotating in a weird manner" — traced to the rotation angle being an arbitrary per-rule number with no real meaning; replaced the whole visualization with a growing comparison table (one fixed `whoAmI` function, called 4 ways, filling a "called as / this becomes / why" table one row at a time) — see spec update in `docs/superpowers/specs/2026-07-04-phase5-7-stepper-expansion-design.md`'s lineage. Standardized a `badge?: string` field on every lesson's View interface so "elaboration" steps (same core picture, deeper explanation) always show a small appearing annotation — the general fix for the "nothing visibly happens on next-click" defect class that the compass bug was one instance of. Every file: hand-verified VIEWS.length === steps.length, then `npm run build` green. Nothing committed — awaiting user browser spot-check. |
| 2026-07-04 | **Phase 5-7 pacing audit + stepper-expansion pilot.** User (a true beginner) felt the "watch it happen" stepper across the newly-built Phase 5/6/7 lessons was paced for someone who already knew JS. Surveyed all 22 built lessons in full (Phase 5: 5.1–5.9, Phase 6: 6.1–6.9, Phase 7: 7.1–7.4): every single one sat at 5-6 steps despite the blueprint's 5-12 range, with captions routinely bundling 2-3 facts into one step/animation-beat — confirmed quantitatively, not just a feeling. Also found: 8 of 22 lessons (5.3, 5.4, 5.7, 5.8, 6.1, 6.4, 6.6, 6.8) keep their metaphor (rope/compass/janitor/spark/receipt/shelf/kitchen-lanes) as the sustained primary narrator for the whole lesson instead of bridging the first mention and handing off to the real term, unlike the other 14 which already follow the Phase-4+ "real terms lead" rule. Wrote design (`docs/superpowers/specs/2026-07-04-phase5-7-stepper-expansion-design.md`) and pilot plan (`docs/superpowers/plans/2026-07-04-phase5-7-stepper-expansion-pilot.md`), then executed the pilot: **5.4 "this"** rewritten 6→9 steps (metaphor bridge-then-fade + fixed the `apply` gap — it was named in the checklist but never explained anywhere in the lesson; this also fixed a pre-existing bug where the lesson had 6 steps but only 5 `VIEWS` entries, silently falling back to the wrong picture on the last step); **6.2 event loop** rewritten 6→10 steps (pure step-splitting; already technical-primary, no metaphor change needed). Two independent drive-by fixes: 7.1 used "listener" three lessons before listeners are taught in 7.4 (reworded); 6.3's underTheHood had a corrupted/stray character (fixed). `npm run build` verified clean after every edit; `VIEWS.length === steps.length` verified by hand for both rewritten lessons (TypeScript can't catch a length mismatch here — `noUncheckedIndexedAccess` is off). Awaiting the user's in-browser pass before rolling the same pattern out to the remaining 20 lessons. |
| 2026-07-04 | **M3 started — Phase 4: 4.1–4.3 built** in the weaned voice (real terms lead). 4.1 Arrays (ArrayShelf: indexed cells, ghost cell past the end, floor-0 fun fact; playlist exercise forces length-1, bans [2]), 4.2 Growing & shrinking (cells physically slide on shift/unshift; returned-value tokens — pop/shift → element, push/unshift → NEW LENGTH, drilled by typed check; todos exercise bans rebuilding the array), 4.3 Objects (ObjectLocker: dot vs bracket-with-note, nested mini-locker, ghost property; dictionary fun fact; pet exercise: self-referential age + space-key brackets). 4.1/4.3 plant the "const but mutated?!" tease → 4.4. Registry: 4.1–4.3 available, 4.4–4.11 planned; home map counts only 'available' as built. 26/26 exercises verified. Same day earlier: Phase 9 Node.js inserted into curriculum (Testing→10, Playwright→11, road grew a bend + "flaky tests ahead" sign), winding-road map + game scenery, LinkedIn footer, Vercel Analytics/SpeedInsights, SEO pass (OG cards, sitemap, JSON-LD), repo moved to Vasavi-alla/javascript-for-babies with history rewritten to Vasavi <vasavireddy.alla@gmail.com>. |
| 2026-07-03 | **Transfer pass on Phases 0–2 + Barnaby upgrades.** (a) Typed checks that quoted watched snippets re-dataed to force tracing (2.1 false-branch flip, 2.5/2.6 fresh loops, 2.4 `0 && "pizza"`, 1.10 new tree, 0.3 quotes-twin); 0.3 exercise computes 6*7, 1.3 upgraded to self-reference sentence-growing. (b) Cat: real 4-frame sprite-cycle walk (contact/passing poses snapped on 130ms timer) + oneko-style chasing (finger/mouse/Pencil-hover leads him along the desk). (c) **Sprite skin support**: drop walk/sit/sleep .webp|.gif in `public/cat/` (side view, facing right, transparent) and Barnaby wears them — probed at mount, all-or-nothing, vector cat is the fallback; video→GIF recipe (unscreen → ezgif/ffmpeg) in `public/cat/README.txt`. |
| 2026-07-03 | **PHASE 3 COMPLETE → M2 done.** Refreshed 3.3–3.5 (MCQs → typed checks; 3.4 gained the ARROW exercise; dry Dartmouth/CoffeeScript facts removed per everyday-hook rule). Built 3.6–3.11 classic: 3.6 CallStackTower (Stack Overflow naming fact), 3.7 ClosureBackpack 🎬 (pays 3.2's "spectacular exception" promise; frame dies, backpack survives), 3.8 callback socket (cashes 3.4's tease; the cheer() vs cheer gotcha as typed check), 3.9 RecursionTower (base case; Google "recursion" Easter egg fact), 3.10 defaults + pure/impure (Excel-is-functional fact), 3.11 tip-calculator composition checkpoint (two-part exercise; return-powered relay; "your first testable unit" bridge to Phase 9). New standing rule applied from 3.8 on: metaphor weaning — real terms lead. All 23 exercises verified self-consistent. Registry 3.6–3.11 → available. |
| 2026-07-03 | **iPad hardening + deploy readiness** (target devices: iPad + laptop). Smart-quote/NBSP normalization on every typing surface (`engine/practice/typing.ts` — iPadOS smart punctuation would fail correct answers and SyntaxError learner code); 16px inputs (kills Safari focus auto-zoom); autocapitalize/correct/complete off on checks + key input; bigger tap areas on stuck/reveal links. Deploy: `_redirects` (Netlify/Cloudflare) + `vercel.json` SPA fallbacks, PWA manifest + apple meta (Add to Home Screen → fullscreen), icon.svg. ⚠️ Deploy rule: NEVER set VITE_GEMINI_KEY in a public build (ships in bundle) — use per-device paste-once flow. Pencil idea parked: teach-back sketch pad (Pointer Events expose pencil pressure/tilt). |
| 2026-07-03 | **Exercises infused into lessons; practice bays removed; fun facts recalibrated.** All 12 drill exercises moved from `/practice/:id` pages into their concept's lesson as `PlayExtra` (same ids → solved-progress persists): 0.3 first-code (new), 1.3 message, 1.4 keyword+counter, 1.5 age, 1.6 fullname, 2.1 weather+oddeven, 2.5 sum, 2.6 countdown, 3.2 +greet-time, 3.3 cheer, 3.5 price-tag (re-themed as scope: outer const). Deleted sets.tsx/PracticePage/route/phase-page section. Fun facts: 3.1's Eich-10-days → ham/hamster naming hustle; V8-car-engine fact (with Ignition/TurboFan kicker) added to 0.2. All 15 exercises verified self-consistent. |
| 2026-07-03 | **Mission format retired; Phase 3 back to classic.** After living with it, the user's verdict: the Machine Shop analogy didn't land — Phase 3 (and 4) will use the SAME classic format as Phases 1–2. Rebuilt 3.1/3.2 as classic lessons keeping the survivors: assembly-line viz (visible gluing work + ConsolePane), typed checks, sticky-note repetition hook, and a CodeExercise (`PlayExtra`) per lesson. Deleted: mission engine (MissionShell/WorkOrderCard/ShopShelf/WhyCard/types), phase-themes, mission31/32, PredictionCard (orphaned), the mission spec. CheckItem + TypeOutputCard moved into `engine/lesson/`. Practice bays unchanged (drill-3a's shop name → "Code Corner"). All 14 exercises re-verified self-consistent. |
| 2026-07-03 | **Typed-checks sweep + silent steppers.** Per user: (a) removed prediction gating from the stepper engine — "watch it happen" plays uninterrupted (useStepper/StepperControls simplified, PredictionCard no longer rendered by LessonShell/Phase1Lab; Step.prediction field kept but dead); (b) converted ~34 quiz MCQs to typed-input checks across phases 0–2 and both mission inspections (TypeOutputCard now app-wide: optional code snippet, custom placeholder, accept-variant lists); MCQs kept only where options are the teaching (~7). Master-plan principle 4 revised: "Type, don't recognize." |
| 2026-07-03 | **Landing page redesigned** (user: "home page is kinda not good") as a notebook spread in three bands: (1) morning page — greeting by hour, single continue card (knows next lesson, resumes mid-mission job steps, links an open practice bay), margin rail (daily sticker + handwritten stats + month calendar); (2) **the road** — 11 phases as a vertical dashed trail with wax-seal stations (teal=cleared, pulsing coral+"you are here"=current, pencil=undrawn), 🏁 finish at "job-ready: automation tester"; (3) the desk — Barnaby full-width at bottom. JourneyPanel deleted; CurriculumMap rewritten. Awaiting user reaction for iteration. |
| 2026-07-03 | **AI mentor everywhere + pacing/motivation.** (a) TeachBack now sends the learner's saved explanation to Gemini for review (judges understanding, never wording); shared MentorPanel extracted, also used by CodeExercise; key lives in gitignored `.env.local` (VITE_GEMINI_KEY) or localStorage — NEVER in source (user pasted it into gemini.ts once; moved out before commit); key verified working against gemini-3.5-flash. (b) BreakCoach (react-idle-timer): active-minutes tracking, 25-min break nudge, 90-min/day stop-for-today nudge, sticky-note overlay; minutes persist in studyLog. (c) Home-page JourneyPanel: day streak (grace until day actually missed), lessons/exercises counters, minutes today, 7-day dots, 36 curated daily notes rotating by date. |
| 2026-07-03 | **Hands-on pivot on top of the mission pivot.** User verdict on first mission build: clicking isn't enough — real code writing is the point. Built `engine/practice/` (worker-sandboxed runner with 2s timeout, CodeExercise with output+syntax inspection, LCS show-answer diff, optional Gemini mentor gemini-3.5-flash on user's own key). Reworked 3.1 per feedback: concrete repetition in Rosa's work order (+ spot-the-difference sticky notes), KT-first blueprint-anatomy step, assemble-with-blanks, GO+rush merged, write-welcome() finale; 3.2 got a write-pickup() finale; both shop-notes converted to sticky trios. Practice bays for Phases 1–3 (9 drill exercises) at `/practice/:id`, listed on phase pages. Exercise wording iterated twice on user feedback: LeetCode-style requirements (WHAT), never step-by-step HOW. All 14 exercises verified self-consistent via node script. |
| 2026-07-03 | **Format pivot for Phases 3–4 approved.** User rejected the playground-first 3.1 pilot as a mere bolt-on and asked for a genuinely different, hook-keeping approach for Phases 3–4. Brainstormed 3 directions; user chose the hybrid: story identity per phase + do-first challenge lessons. Phase 3 = The Machine Shop (work orders, alternating job types, shop shelf → 3.11 assembles the shelf into the tip calculator), Phase 4 = The Warehouse (4.4 references as the "someone changed my crate" mystery; checkpoint = warehouse audit). Spec written: `docs/superpowers/specs/2026-07-03-mission-format-phases-3-4-design.md` (old format-variety spec deleted/superseded). Phases 0–2 untouched. Next session: build the 3.1 + 3.3 pilot pair. |
| 2026-07-02 | Project kickoff. Decisions made: warm-sketchbook UI, phased interaction (guided → sandbox), Playwright as the automation target. Wrote plan files 00–05 and CLAUDE.md. No code yet. |
| 2026-07-02 | M0 built: Vite+React+TS scaffold, sketchbook design system (rough.js primitives, PaperCard, StickyNote, InkButton, TapeLabel, HighlightMark, CodePane/shiki, ConsolePane), stepper engine with prediction gates, lesson registry, design lab (`/design`) with a live lesson-1.2 preview. User feedback after first run: map copy too vague for newbies → added per-phase pages (`/phase/:n`) with plain-words intros + key-term glossaries (`src/content/phase-intros.ts`), elevated Phases 0–1 as "the foundation", explicitly covered sync/async/blocking/non-blocking/Node concepts in phase intros. |
| 2026-07-02 | Second feedback round: added "why do we need this?" sections to all phase intros (variables especially); made labs per-phase (`src/labs/`: Phase0Lab = big-picture code→engine→memory→output diagram, Phase1Lab = variable-birth stepper moved out of the common gallery); `/design` demoted to internal style guide; rewrote the reassignment prediction in plain standalone language (no box/label-metaphor dependence). |
| 2026-07-02 | Built the full lesson framework (LessonShell 6-part anatomy, QuizCard, TeachBack + journal, progress store with localStorage, done-badges on phase pages and map) and ALL of Phase 0: 0.1 InstructionTape, 0.2 EngineDiagram, 0.3 ConsoleViz + live TryConsole, 0.4 MemoryWall, 0.5 ErrorAnatomy. User verified Phase 0 working. Then Phase 1 lessons 1.1 (ValueZoo), 1.2 (MemoryDiagram hero lesson), 1.3 (Reassignment with evaluation bubble). Session ended near usage limit; next: 1.4–1.11. |
| 2026-07-03 | **Started Phase 3 (M2 second half)** — built 3.1 what-is-a-function (FunctionMachine 🎬: hopper/gear/chute, define-vs-call as the core beat, "nothing printed yet" prediction; Eich/Scheme 10-days fun fact), 3.2 params vs args (two labeled slots, positional matching, fresh-slots-per-call, missing-arg→undefined prediction; ALGOL 60 formal/actual-parameters fun fact), 3.3 return (returned value travels back and REPLACES the call at a call-site strip; sealed-chute machine for the console.log-vs-return confusion; window-vs-chute picture; Dartmouth BASIC 1964 GOSUB/RETURN fun fact), 3.4 function expressions & arrows (functions-are-values: memory slots holding tiny machines, typeof→"function" prediction, braces-kill-auto-return quiz gotcha; CoffeeScript => fun fact). Registry now lists all Phase 3 lessons (3.5–3.11 'planned'). Also fixed 3 pre-existing TS unused-import errors in phase 2 lessons (22, 24, 27) that broke `npm run build`; build now green. 3.1–3.4 not yet user-verified in browser. |
| 2026-07-03 | **Phase 2 complete**: 2.7 break/continue/nested (station track with ejector seat & skip ramp, grid-multiplication viz, test-matrix quiz, Dijkstra goto fun fact), 2.8 FizzBuzz checkpoint (gate corridor walk per number, the gate-order trap prediction, full 1–15 rhythm grid, interactive FizzBuzzExplorer with Jazz-for-7 challenge, Imran Ghory/Jeff Atwood fun fact). |
| 2026-07-03 | Fixed scroll-to-top on route change (ScrollToTop in App.tsx — React Router keeps scroll position otherwise). Built 2.4 ternary & short-circuit (value-picking fork, || default rail with the 0-stomping trap + ?? fix, && guard; C-1972 vs ??-2020 fun fact), 2.5 while (circular road + gate, laps, infinite-loop freeze; Halting Problem fun fact), 2.6 for/LoopMachine (three slots with schedules, odometer, checks=laps+1 prediction; FORTRAN "i" fun fact). |
| 2026-07-03 | Started M2 / Phase 2: 2.1 if/else (FlowRoad — traveling token, grayed untaken road, CPU branch-prediction fun fact), 2.2 truthy/falsy (Boolean-izer funnel, the falsy six, "0"/" " surprises, document.all fun fact), 2.3 else-if & switch (gate corridor, first-true-wins prediction, switch ladder with fall-through, AT&T 1990 fun fact). Registry now lists Phase 2 lessons (2.4–2.8 planned). User requested standing "fun facts" rule → saved to memory + Next up. |
| 2026-07-02 | Finished Phase 1 → **M1 complete**: 1.4 (ConstScene: padlocked label + TypeError bounce + var ghost), 1.5 (NumberLineViz: hops, %, the 0.1+0.2 microscope), 1.6 (StringTrain: indexed cars, gluing, template slots), 1.7 (NothingScene: undefined vs null, typeof-null gotcha), 1.8 (TypeofScene: type tags live on values), 1.9 (CoercionMachine: "5"+5 vs "5"-5, == vs ===), 1.10 (ExpressionTree: leaves-to-root evaluation, short-circuit, precedence-as-tree), 1.11 (checkpoint: Mad Libs — CheckpointScene + interactive MadLibsBuilder that writes the user's program from their inputs + real-console F12 graduation challenge). Also answered user's objects question: references/call-by-value-vs-reference live in Phase 4 lesson 4.4 (curriculum doc updated to make function-argument passing explicit). |
